service vendor.light-default /vendor/bin/hw/android.hardware.light-service.mt6765-common
    class hal
    user system
    group system
    # shutting off lights while powering-off
    shutdown critical
